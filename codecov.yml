coverage:
  status:
    project:
      default:
        target: 100
        threshold: 10
        paths: 'lib/'
    patch:
      default:
        target: 100
        threshold: 10
        paths: 'lib/'
